# TaskTrackr

A lightweight full-stack task manager. HTML/CSS/JS front end talks to a small PHP backend
(`api/tasks.php`) that stores tasks as JSON on disk.

## Run locally

```
php -S localhost:8000
```

Then open http://localhost:8000 in your browser.

> Full setup/usage docs to be expanded by QA/DevOps (Student 5).
